# ============================================================
# Automated Reporting Platform — Dependencies
# ============================================================

# API framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Data processing
pandas>=2.0.0
openpyxl>=3.1.0

# Document extraction
pdfplumber>=0.10.0
python-docx>=1.1.0

# PDF generation
reportlab>=4.1.0

# Background scheduling
APScheduler>=3.10.0

# Settings and environment management
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0
python-dotenv>=1.0.0

# Email validation
email-validator>=2.0.0

# Database
sqlalchemy>=2.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# Pin bcrypt: passlib 1.7.4 is incompatible with bcrypt >= 4.1
# (registration fails with "password cannot be longer than 72 bytes").
bcrypt<4.1

# AI insights (optional — falls back to a rule-based generator if no API key)
openai>=1.0.0
